import styled from 'styled-components';

export const Container = styled.button`
  display: flex;
  align-items: center;
  background: var(--primary);
  min-height: 40px;
  border: none;
  border-radius: 30px;
  padding: 10px 40px;

  font-weight: bold;
  color: #fff;
`;
